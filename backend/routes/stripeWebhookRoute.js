// import express from "express";
// import { stripeWebhook } from "../controllers/stripeWebhookController.js"; // if using webhook

// const router = express.Router();

// // For production you can use Stripe webhook
// router.post(
//   "/stripe-webhook",
//   express.raw({ type: "application/json" }),
//   stripeWebhook
// );

// export default router;